import { _decorator, Asset, Component, instantiate, Node, Prefab, ProgressBar } from 'cc';
const { ccclass, property } = _decorator;

/** 入口文件 */
@ccclass('Main')
export class Main extends Component {

    // 加载
    onLoad() {
    }

    // 开始
    start() {
    }

}
